import { Layout } from './Layout';

// Re-export NavItem type
export type { NavItem } from './types';

// Export Layout component both as default and named export
export { Layout };
export default Layout;
